# Invariants

These are promises the parent skill should keep unless the user explicitly overrides them.

- Complete the user's requested task before spending extra effort on self-improvement.
- Apply at most one new process experiment per invocation.
- Prefer small, reversible changes over broad speculative rewrites.
- Keep historical evidence append-only in `RUN_LOG.jsonl`.
